spring:
  application:
    name: dseum-board
  config:
    import:
      - "optional:configserver:http://configserver:8071"
      - "vault://"

  cloud:
    vault:
      host: dseum-vault
      port: 8200
      scheme: http
      authentication: TOKEN
      token: eum-root
      kv:
        enabled: true
        backend: secret
        default-context: application
        profile-separator: "/"

    aws:
      region:
        static: ${cloud.aws.region.static:ap-northeast-2} # 👈 값 없으면 서울로 기동
      credentials:
        access-key: ${cloud.aws.credentials.access-key}
        secret-key: ${cloud.aws.credentials.secret-key}
      s3:
        bucket: ${cloud.aws.s3.bucket}

eureka:
  client:
    serviceUrl:
      defaultZone: http://eurekaserver:8070/eureka/

# 프로메테우스 설정
management:
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus"

  endpoint:
    health:
      access: read_only
      show-details: always
    prometheus:
      access: read_only

  metrics:
    tags:
      # 💡 위에서 설정한 고유한 name을 자동으로 물고 들어갑니다. (무결성 포인트)
      application: ${spring.application.name}
  tracing:
    sampling:
      # 💡 1.0 = 모든 요청을 100% 추적 (개발 중에는 무결성을 위해 1.0 권장)
      # 운영 중에는 9060XT급 부하를 줄이기 위해 0.1(10%) 정도로 조절합니다.
      probability: 1.0
    zipkin:
      tracing:
        # 💡 도커 컴포즈에 설정한 zipkin-server 주소와 포트
        endpoint: "http://zipkin-server:9411/api/v2/spans"
