package com.eum.paymentserver.service;

/**
 * TossPayments 웹훅은 현재 프로젝트 범위에서 사용하지 않는다.
 *
 * 향후 웹훅을 다시 도입할 때 새 계약에 맞춰 별도 구현한다.
 */
public final class PaymentWebhookService {

    private PaymentWebhookService() {
    }
}
