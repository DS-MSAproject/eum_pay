package com.eum.paymentserver.controller;

/**
 * TossPayments 웹훅은 현재 프로젝트 범위에서 사용하지 않는다.
 *
 * 웹훅 경로를 의도적으로 비활성화하기 위해 컨트롤러 빈 등록을 제거했다.
 */
public final class PaymentWebhookController {

    private PaymentWebhookController() {
    }
}
